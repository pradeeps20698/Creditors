[theme]
# Pin a dark theme so tables/colors render identically in local dev and on
# Streamlit Cloud. All AgGrid cell styles in app.py are tuned for a dark base.
base = "dark"
primaryColor = "#4aa3ff"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#1e2530"
textColor = "#fafafa"
